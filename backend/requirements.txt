# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.35
psycopg[binary]==3.2.3
alembic==1.13.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Redis
redis==5.1.1

# Rate Limiting
slowapi==0.1.9

# Email
aiosmtplib==3.0.2

# Push Notification
pywebpush==2.0.0

# Utilities
python-dotenv==1.0.1
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Celery
celery==5.4.0

# Socket.IO (team/project channel chat — still in use)
python-socketio==5.11.4

# Native WebSocket (DM private chat)
websockets==13.1

# HTTP Client
httpx==0.27.2

# Error monitoring (optional — only initialized if SENTRY_DSN_BACKEND is set)
sentry-sdk[fastapi]==2.18.0
